# Dolby
allow mediacodec hal_dms_hwservice:hwservice_manager find;
hal_client_domain(mediacodec, hal_dms)
get_prop(mediacodec, graphics_config_prop)

# Private
# get_prop(mediacodec, custom_version_prop)
# get_prop(mediacodec, misctrl_prop)
# get_prop(mediacodec, system_prop)
